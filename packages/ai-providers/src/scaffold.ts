export const SCAFFOLD_MODELS = {
  /** Default Anthropic model prefilled into a scaffolded config. */
  anthropic: "claude-sonnet-4-6",
  /** Default OpenAI model prefilled into a scaffolded config. */
  openai: "gpt-5.4-mini",
  /** Default Gemini model prefilled into a scaffolded config. */
  gemini: "gemini-2.5-flash",
} as const;
