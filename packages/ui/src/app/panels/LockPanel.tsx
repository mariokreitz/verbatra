import type { ReactNode } from "react";

/** Placeholder: a later view ticket replaces this with the lock-drift view. */
export function LockPanel(): ReactNode {
  return <p>Lock view coming soon.</p>;
}
