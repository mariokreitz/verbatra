import type { ReactNode } from "react";

/** Placeholder: a later view ticket replaces this with the detailed pending-change view. */
export function DiffPanel(): ReactNode {
  return <p>Diff view coming soon.</p>;
}
