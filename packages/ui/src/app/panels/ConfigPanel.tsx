import type { ReactNode } from "react";

/** Placeholder: a later view ticket replaces this with the read-only config and glossary viewer. */
export function ConfigPanel(): ReactNode {
  return <p>Config view coming soon.</p>;
}
