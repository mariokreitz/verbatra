import type { ReactNode } from "react";

/** Placeholder: a later view ticket replaces this with the aggregate drift status view. */
export function StatusPanel(): ReactNode {
  return <p>Status view coming soon.</p>;
}
