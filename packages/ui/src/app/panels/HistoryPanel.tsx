import type { ReactNode } from "react";

/** Placeholder: a later view ticket replaces this with the git-log history view. */
export function HistoryPanel(): ReactNode {
  return <p>History view coming soon.</p>;
}
