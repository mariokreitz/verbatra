---
title: Fichier de configuration
description: Chaque clé de configuration avec son type, son défaut et ses contraintes, l'ordre de découverte, comment un projet sans fichier de configuration est détecté, et ce qui se passe quand la validation échoue.
---
verbatra lit un petit objet de configuration sans secret et le valide avant de faire le moindre travail. Cette page est la référence complète : chaque clé, où le fichier peut vivre, et comment une mauvaise configuration échoue. Le fichier lui-même est optionnel : sans configuration nulle part, verbatra [en déduit une depuis le projet](#fonctionner-sans-fichier-de-configuration) plutôt que de refuser de tourner.

<Callout title="Les clés viennent de l'environnement">
Aucune clé d'API n'est jamais lue depuis ce fichier. Chaque fournisseur lit sa clé depuis une variable d'environnement, donc la configuration peut être commitée sans risque. Voir [Fournisseurs](/docs/providers).
</Callout>

## Une configuration minimale

```ts
import { defineConfig } from "@verbatra/sdk";

export default defineConfig({
  sourceLocale: "en",
  targetLocales: ["de", "fr"],
  format: "i18next-json",
  files: {
    pattern: "locales/{locale}.json",
  },
  provider: {
    id: "gemini",
    options: {
      model: "gemini-2.5-flash", // example model id; use your provider's current one
      maxOutputTokens: 4096,
    },
  },
});
```

`defineConfig` est un assistant identité de `@verbatra/sdk`. Il renvoie son argument tel quel et n'existe que pour te donner l'inférence de types et l'autocomplétion de l'éditeur.

<Callout title="Autocomplétion des modèles en TypeScript">
Quand tu écris la configuration en TypeScript, ton éditeur propose pour `options.model` les identifiants de modèle connus du fournisseur sélectionné, restreints selon l'`id` de fournisseur que tu as choisi. La liste vient du propre SDK de ce fournisseur, donc elle reste à jour avec la version du SDK que tu as installée, et un modèle d'un autre fournisseur (par exemple un modèle Claude sous `id: "gemini"`) est une erreur de type. C'est une aide à l'écriture uniquement : à l'exécution, `model` est validé comme une chaîne non vide, donc un tout nouveau modèle que le SDK n'a pas encore listé fonctionne quand même, même si l'éditeur le signale. DeepL n'a pas de champ `model`, et le `model` d'openai-compatible est ce que ton serveur expose, donc ni l'un ni l'autre n'est restreint.
</Callout>

## Chaque clé

| Clé | Type | Défaut | Rôle |
| --- | --- | --- | --- |
| `sourceLocale` | chaîne | requis | La locale dans laquelle tes chaînes source sont écrites. |
| `targetLocales` | tableau de chaînes | requis | Les locales vers lesquelles traduire. |
| `format` | chaîne | requis | L'adaptateur de format pour tes fichiers de locale. |
| `files.pattern` | chaîne | requis | Le chemin de chaque fichier de locale, avec un jeton `{locale}`. |
| `files.localeStyle` | `"literal"`, `"posix"` ou `"android"` | `"literal"` | Comment le jeton `{locale}` est écrit pour chaque locale. |
| `provider` | objet | requis | Le fournisseur de traduction, sélectionné par `id`. |
| `glossary` | objet ou chaîne | aucun | Termes source vers termes cibles préférés, en ligne ou en chemin de fichier JSON. |
| `tone` | `"formal"`, `"informal"` ou `"neutral"` | aucun | Le registre que les traductions doivent employer. |
| `prune` | booléen | `false` | Supprime les clés orphelines des fichiers cibles et du fichier de verrouillage. |
| `generatePlurals` | booléen | `false` | Synthétise les formes plurielles CLDR dont une langue cible a besoin. |
| `maxBatchSize` | entier positif | `50` | Le maximum d'entrées envoyées dans une seule requête fournisseur. |
| `maxTokens` | entier positif | aucun | Un budget de tokens pour toute l'exécution, sur chaque appel fournisseur. |
| `budgetBehavior` | `"warn"` ou `"stop"` | `"warn"` | Ce qui se passe quand `maxTokens` est atteint. |
| `$schema` | chaîne | aucun | Un pointeur vers le document JSON Schema, pour les éditeurs seulement. Ignoré à l'exécution. |

Les clés de premier niveau inconnues sont rejetées, à la seule exception de `$schema`. C'est aussi pourquoi une clé d'API ne peut pas vivre dans la configuration : les clés viennent de l'environnement, pas du fichier.

<AvailableFrom version="0.9.0" />

### Locales et fichiers

- **`sourceLocale`** doit être une chaîne non vide.
- **`targetLocales`** doit contenir au moins une locale, ne doit pas inclure `sourceLocale`, et deux entrées ne peuvent pas être égales sans tenir compte de la casse (par exemple `["de", "DE"]` est rejeté), puisque chaque locale devient le nom de sa propre feuille Excel à l'export.
- **`format`** est l'un de `i18next-json`, `vue-i18n-json`, `next-intl-json`, `ngx-translate-json`, `xliff`, `yaml`, `arb` ou `properties`. Voir [Formats](/docs/formats).
- **`files.pattern`** doit contenir le jeton `{locale}`, que verbatra remplace par chaque locale : `locales/{locale}.json` se résout en `locales/de.json`. Les chemins se résolvent contre le répertoire de travail. Le motif est un chemin littéral, pas un glob, et `{locale}` est le seul jeton qu'il comprend, donc une configuration adresse exactement un fichier par locale. Si ton projet répartit ses chaînes sur plusieurs fichiers d'espace de noms par locale, voir [Dispositions avec espaces de noms](/docs/formats#dispositions-avec-espaces-de-noms).
- **`files.localeStyle`** décide en quoi le jeton se développe. `literal`, la valeur par défaut, utilise la balise configurée telle quelle, ce dont ont besoin les dispositions JSON, YAML, XLIFF, ARB, properties et `.lproj` d'Apple. `posix` remplace `-` par `_`, pour gettext (`locale/pt_BR/LC_MESSAGES/messages.po`) et la disposition Java `messages_{locale}.properties`. `android` développe le jeton en un répertoire de ressources Android complet, écrit `res/{locale}/strings.xml` : `values` pour la locale source, puis `values-de`, `values-pt-rBR` et `values-b+zh+Hans`. Un style refuse une locale pour laquelle il n'a pas d'écriture correcte, plutôt que d'écrire un répertoire que la plateforme ignorerait en silence.

### provider

Un objet sélectionné par `id` : `anthropic`, `openai`, `gemini`, `deepl` ou `openai-compatible`. Chaque fournisseur prend ses propres `options` (modèle, limite de tokens, et champs spécifiques au fournisseur), et les clés d'option inconnues sont rejetées. Les options par fournisseur sont documentées sur [Fournisseurs](/docs/providers).

### glossary

Une correspondance de termes source vers des termes cibles préférés, soit en ligne, soit comme chemin vers un fichier JSON de la même forme (un objet plat de clés chaînes vers valeurs chaînes) :

```ts
export default defineConfig({
  // ...
  glossary: {
    "Sign in": "Anmelden",
  },
  // or: glossary: "glossary.json",
});
```

Les deux formes sont mutuellement exclusives ; il n'y a pas de fusion entre elles. Un chemin de fichier relatif se résout contre le répertoire du fichier de configuration chargé, ou contre le répertoire de travail quand la configuration est passée comme surcharge en mémoire. Le fichier doit être encodé en UTF-8 et ne pas dépasser 1 MiB, et il est lu une fois au chargement de la configuration : verbatra ne le surveille pas, donc modifie-le et redémarre pour prendre en compte une mise à jour. Un fichier absent, un JSON invalide ou une valeur qui n'est pas une carte plate de chaînes fait échouer le chargement de la configuration avec `CONFIG_INVALID`, en nommant le chemin résolu.

<AvailableFrom version="0.4.0" pkg="@verbatra/studio" />

Les deux formes diffèrent aussi par qui peut les modifier. Seule la forme fichier se modifie depuis [Verbatra Studio](/docs/review-in-studio), qui réécrit ce fichier JSON terme par terme et ne touche jamais à ton module de config ; un glossaire inline y reste en lecture seule, car il vit dans du code exécutable. Préfère la forme fichier si tu veux gérer les termes depuis le tableau de bord.

La façon dont le glossaire est appliqué dépend du fournisseur : les fournisseurs LLM reçoivent la carte résolue avec la requête et ont pour instruction de traiter ses termes comme contraignants, tandis que DeepL ignore une carte de termes et applique à la place un glossaire configuré par id. Voir [Fournisseurs](/docs/providers).

### tone

L'un de `"formal"`, `"informal"` ou `"neutral"`. Les fournisseurs LLM le reçoivent avec la requête et ont pour instruction de le respecter ; DeepL le mappe sur son réglage de formalité. Voir [Fournisseurs](/docs/providers) pour les détails par fournisseur, y compris la dégradation de l'offre gratuite de DeepL.

### prune

Désactivé par défaut. Quand il vaut true, les clés présentes dans un fichier cible mais absentes de la source (les clés orphelines de la comparaison) sont supprimées du fichier écrit et du [fichier de verrouillage](/docs/the-lock-file). Seules les clés orphelines sont jamais supprimées. Une option `prune` par exécution sur `translate` (l'option CLI `--prune`) prime sur la valeur de la configuration.

### generatePlurals

Désactivé par défaut. Quand il vaut true, verbatra synthétise les formes plurielles CLDR qu'une langue cible exige mais que la source ne fournit pas (par exemple few et many pour le polonais). Pris en charge uniquement pour les projets i18next-JSON traduits par un fournisseur LLM ; DeepL, les formats non i18next et les langues cibles inconnues retombent sur l'avertissement de pluriel par locale. Il n'y a pas d'option CLI, donc règle ça dans la configuration ; l'entrée de `translate()` dans le SDK accepte une surcharge `generatePlurals` par exécution, qui prime.

### maxBatchSize

Défaut `50`. Les entrées manquantes plus modifiées d'une locale sont découpées en sous-lots séquentiels pas plus grands que ça, pour qu'une requête fournisseur trop grosse ne puisse pas faire échouer toute la locale. Un sous-lot échoué est retenu et retenté à l'exécution suivante pendant que les autres avancent quand même. Doit être un entier positif ; zéro, négatif ou fractionnaire est rejeté. Configuration uniquement : pas d'option CLI ni de surcharge par exécution.

### maxTokens et budgetBehavior

`maxTokens` est un budget de tokens pour toute l'exécution : tokens d'entrée plus de sortie, sommés sur chaque appel fournisseur (traduction principale et génération de pluriels confondues) et sur chaque locale cible. Il est vérifié après chaque sous-lot terminé, jamais à l'intérieur d'un, donc le sous-lot dont l'achèvement franchit le plafond a déjà été envoyé, est accepté normalement, et ses tokens comptent quand même : un plafond souple a posteriori, pas une pré-vérification dure. Configuration uniquement, pas d'option CLI.

`budgetBehavior` décide de ce qui se passe une fois le plafond atteint :

- `"warn"` (le défaut) signale le dépassement et laisse l'exécution continuer exactement comme si aucun budget n'était configuré.
- `"stop"` retient chaque clé pas encore tentée pour le reste de l'exécution : les candidates restantes de la locale en cours, et entièrement les candidates de chaque locale cible suivante (leur comparaison et leur rapport d'orphelines tournent quand même ; seul l'appel fournisseur est sauté). Les clés retenues gardent leur hash de verrouillage précédent et sont retentées automatiquement à l'exécution suivante, comme un appel fournisseur échoué l'est déjà.

Définir `maxTokens` contraint aussi la parallélisation d'une exécution : une exécution réelle qui le combine avec `--concurrency` supérieur à `1` est refusée avec le code de sortie `2` (`CONCURRENCY_BUDGET_CONFLICT`) avant tout appel fournisseur, car des locales concurrentes ne peuvent pas respecter un plafond de tokens de façon déterministe. Un dry run est exempté. Retire le budget ou laisse la concurrence à `1`.

Sous `"warn"`, un déclenchement du budget ne change jamais le code de sortie de la commande. Sous `"stop"`, il peut le faire : une locale dont des clés ont été retenues est rapportée comme `partial` quand certaines clés sont bien arrivées et `failed` quand aucune n'est arrivée, et les deux sortent avec `1`. `budgetBehavior` sans `maxTokens` est accepté et n'a aucun effet. Face à un fournisseur sans tokens (DeepL, qui ne rapporte aucun usage à mesurer), le garde-fou se déclare inerte (`supported: false` sur le `budget` du résumé d'exécution) plutôt que de produire un faux déclenchement.

<Callout title="Deux maxTokens différents">
Le `maxTokens` de premier niveau est le budget de toute l'exécution décrit ici. Le fournisseur Anthropic a aussi son propre `options.maxTokens`, qui plafonne une seule réponse. Ils n'ont aucun lien ; voir [Fournisseurs](/docs/providers).
</Callout>

## Ordre de découverte

verbatra cherche vers le haut depuis le répertoire de travail et utilise la première source qu'il trouve. Plusieurs sources présentes ne sont pas une erreur ; la première correspondance gagne. L'ordre de recherche est :

1. `package.json` (une propriété `"verbatra"`)
2. `.verbatrarc`
3. `.verbatrarc.json`
4. `.verbatrarc.yaml`
5. `.verbatrarc.yml`
6. `.verbatrarc.js`
7. `.verbatrarc.cjs`
8. `.verbatrarc.ts`
9. `verbatra.config.js`
10. `verbatra.config.cjs`
11. `verbatra.config.ts`

Pour charger un fichier explicite au lieu de chercher, passe `--config <path>` ; un chemin relatif se résout contre le répertoire de travail. Dans le SDK, un `configOverride` en mémoire prime sur un `configPath` explicite, qui prime sur la recherche. Voir [API programmatique](/docs/programmatic-api).

## Fonctionner sans fichier de configuration

<AvailableFrom version="0.10.0" />

Un fichier de configuration n'est plus un prérequis. Quand la recherche ci-dessus ne trouve rien, verbatra déduit une configuration du projet au lieu d'échouer, si bien que [`verbatra check`](/docs/cli/check) et [`verbatra diff`](/docs/cli/diff) tournent dans un projet jamais configuré, et que [`verbatra translate`](/docs/cli/translate) et [`verbatra watch`](/docs/cli/watch) n'ont besoin que d'une clé d'API de provider dans l'environnement. `export` et `import` sont détectés de la même façon.

Une configuration que tu as écrite l'emporte toujours. La détection ne tourne que lorsque la recherche revient vide, et un `--config <path>` explicite qui n'existe pas reste une erreur `CONFIG_NOT_FOUND` plutôt qu'un glissement vers une supposition sur un autre projet.

La détection n'est jamais silencieuse. Avant l'exécution de la commande, elle écrit sa conclusion sur stderr :

```
verbatra: no config file found, so the project was detected automatically.
  files:   src/assets/i18n/{locale}.json (ngx-translate-json)
  locales: en -> de
  no provider API key found
  Run "verbatra init" to write this down as a config you control.
```

Avec `--json`, la même information tient dans une seule ligne `{"event":"detection", ...}` sur stderr, ce qui garde stdout comme une [enveloppe de résultat](/docs/ci-and-exit-codes#sortie-json) propre.

### Ce que la détection regarde

**Répertoires.** Seuls ceux-ci sont lus, dans cet ordre :

```
locales, src/locales, public/locales, messages, src/messages, i18n, src/i18n,
src/assets/i18n, assets/i18n, public/assets/i18n, app/locales, lang, translations,
config/locales, lib/l10n
```

Rien en dehors de cette liste n'est jamais ouvert, et c'est pourquoi `node_modules` et la sortie de build ne sont jamais parcourus. Deux d'entre eux portant une disposition utilisable est un échec, pas une supposition.

**Disposition.** Un répertoire n'est accepté que lorsqu'un seul motif de chemin couvre chaque fichier porteur de locale qu'il contient et que ce motif couvre au moins deux locales. Les deux formes que les vrais projets utilisent fonctionnent : des fichiers nommés d'après la locale (`locales/en.json`, `messages/en.json`, `lib/l10n/app_en.arb`) et un fichier d'espace de noms par répertoire de locale (`public/locales/en/common.json`). Un fichier sans code de locale, un `README.md` par exemple, est ignoré plutôt que compté contre le répertoire.

**Codes de locale.** Un sous-tag de langue à deux lettres, éventuellement avec une écriture, une région, ou les deux : `en`, `de`, `pt-BR`, `zh-Hans`. Les codes de langue à trois lettres et les graphies POSIX avec tiret bas (`pt_BR`) ne sont pas reconnus, ces projets ont donc besoin d'une configuration.

**Format.** `.xlf`, `.xliff`, `.yml`, `.yaml`, `.arb` et `.properties` se résolvent par l'extension du fichier. `.json` non, parce que quatre formats la partagent : il se résout par la bibliothèque i18n déclarée dans `package.json`, à savoir `i18next`, `vue-i18n`, `next-intl` ou `@ngx-translate/core`. Une disposition `.json` qui n'en nomme aucune, ou qui en nomme deux, n'est pas détectée.

**Locale source.** D'abord `en` exact, puis n'importe quel `en-*`. Toute autre locale détectée devient une cible. Ce biais vers l'anglais est un choix de conception délibéré, pas un défaut neutre : les alternatives (la première par ordre alphabétique, ou la locale ayant le plus de clés) peuvent se tromper en silence, et une mauvaise locale source traduit dans le mauvais sens. Un projet dont la locale source n'est pas l'anglais n'est pas cassé, il lui faut simplement une configuration, et l'erreur le dit.

**Provider.** Choisi parmi les clés d'API réellement définies, dans l'ordre fixe `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `GEMINI_API_KEY`, `DEEPL_API_KEY`. L'ordre est fixe et ne dépend pas de l'environnement, pour que le même projet se résolve au même provider sur chaque machine et en CI ; si plusieurs clés sont définies, la première de cet ordre l'emporte et la ligne de transparence nomme les autres. `openai-compatible` n'est jamais détecté, parce que toi seul peux fournir sa `baseUrl`. Sans aucune clé, `check` et `diff` tournent quand même et `translate --dry-run` prévisualise toujours ; `translate` et `watch` échouent avec `PROVIDER_KEY_MISSING`.

### Ce que la détection ne fait pas

Elle ne synthétise que les clés obligatoires : `sourceLocale`, `targetLocales`, `format`, `files.pattern` et `provider` (le modèle de scaffold par défaut du provider, avec une limite de tokens de sortie de `4096` ; DeepL ne prend ni l'un ni l'autre). Tout le reste garde son défaut : une exécution détectée n'a donc ni `glossary`, ni `tone`, ni `prune`, ni `generatePlurals`, ni budget `maxTokens`, et `files.localeStyle` vaut `"literal"`. Chacun de ces réglages suppose d'écrire une configuration : lance `verbatra init` pour poser sur disque le setup détecté et pars de là.

Elle décline aussi plutôt que de deviner. Chaque refus nomme un remède, et la CLI sort avec `2` :

| Code | Quand |
| --- | --- |
| `PROJECT_NOT_DETECTED` | aucun répertoire candidat ne contenait un ensemble reconnaissable de fichiers de locale, le format n'a pas pu être résolu, ou aucune locale détectée n'est l'anglais |
| `PROJECT_AMBIGUOUS` | deux répertoires candidats portent tous deux une disposition utilisable, par exemple `locales/` et `public/locales/` |
| `PROJECT_LAYOUT_UNSUPPORTED` | le répertoire a besoin de plus d'un motif de chemin, ce à quoi ressemblent plusieurs espaces de noms par locale ; verbatra fait correspondre un fichier par locale (voir [Dispositions avec espaces de noms](/docs/formats#dispositions-avec-espaces-de-noms)) |
| `PROVIDER_KEY_MISSING` | le projet a été détecté et aucune clé d'API de provider supportée n'est définie, sur une commande qui appellerait un provider |

[`verbatra doctor`](/docs/cli/doctor) reste délibérément en dehors de tout cela : il valide une configuration que tu as écrite, donc dans un projet qui n'en a pas, il rapporte sa vérification de configuration comme échouée au lieu d'en détecter une. [`verbatra studio`](/docs/cli/studio) a lui aussi besoin d'un fichier de configuration.

## Validation dans l'éditeur

<AvailableFrom version="0.9.0" />

`@verbatra/sdk` livre le schéma de configuration sous forme de document JSON Schema, généré à la compilation depuis le schéma zod même contre lequel le SDK valide : il ne peut donc jamais décrire une forme différente de celle qui s'exécute. Une fois installé, il se trouve à :

```
node_modules/@verbatra/sdk/dist/config-schema.json
```

Pointe ton éditeur dessus et une configuration déclarative gagne la complétion des clés, la vérification des types et des valeurs d'énumération, et un avertissement immédiat sur une clé mal orthographiée.

Il faut indiquer à l'éditeur quel document utiliser ; rien ne se valide tout seul. Il y a deux façons de le faire.

**Une clé `$schema` dans le fichier.** `.verbatrarc.json` et `.verbatrarc` acceptent une clé `$schema` de premier niveau, que verbatra admet et ignore :

```json
{
  "$schema": "./node_modules/@verbatra/sdk/dist/config-schema.json",
  "sourceLocale": "en",
  "targetLocales": ["de", "fr"],
  "format": "i18next-json",
  "files": { "pattern": "locales/{locale}.json" },
  "provider": { "id": "gemini", "options": { "model": "gemini-2.5-flash", "maxOutputTokens": 4096 } }
}
```

La clé `$schema` est une convention d'éditeur, pas une exigence de la spécification JSON Schema : VS Code et les éditeurs qui suivent la même convention la lisent, les autres non. `.verbatrarc` n'a pas d'extension, donc l'éditeur y a besoin en plus d'une entrée de correspondance de fichiers avant de traiter le fichier comme du JSON ; la clé seule n'y suffit pas.

**Une correspondance explicite dans l'éditeur.** Dans VS Code, associe plutôt les fichiers dans les réglages du plan de travail, ce qui couvre aussi la forme sans extension :

```json
{
  "json.schemas": [
    {
      "fileMatch": [".verbatrarc", ".verbatrarc.json"],
      "url": "./node_modules/@verbatra/sdk/dist/config-schema.json"
    }
  ]
}
```

Une configuration YAML (`.verbatrarc.yaml`, `.verbatrarc.yml`) prend un commentaire plutôt qu'une clé, lu par le YAML Language Server :

```yaml
# yaml-language-server: $schema=./node_modules/@verbatra/sdk/dist/config-schema.json
sourceLocale: en
targetLocales: [de, fr]
```

Deux des emplacements de recherche ne peuvent pas être servis. La propriété `"verbatra"` dans le `package.json` ne le peut pas, parce que le `package.json` valide déjà contre son propre schéma et qu'il n'existe aucun moyen d'en attacher un second à une seule propriété. Les formes exécutables (`.verbatrarc.js`, `.verbatrarc.cjs`, `.verbatrarc.ts`, `verbatra.config.js`, `verbatra.config.cjs`, `verbatra.config.ts`) n'en ont pas besoin : `defineConfig` leur donne le type `VerbatraConfigInput` complet, donc TypeScript les vérifie déjà, avec en prime la complétion des modèles restreinte au fournisseur choisi.

### Ce que le document ne peut pas vérifier

Le document porte chaque clé, son type, ses valeurs d'énumération, les clés obligatoires, la règle du jeton `{locale}` sur `files.pattern` et le rejet des clés inconnues. Trois règles sont appliquées à l'exécution mais n'ont aucune expression dans le brouillon 2020-12 de JSON Schema : l'éditeur reste donc muet à leur sujet, et verbatra les signale au chargement de la configuration.

- `targetLocales` ne doit pas inclure `sourceLocale`. Comparer deux valeurs sœurs n'a aucun vocabulaire dans le brouillon.
- `targetLocales` ne doit pas contenir deux locales qui ne diffèrent que par la casse.
- `provider.options.apiKeyEnvVar` (openai-compatible seulement) ne doit pas nommer la variable d'environnement d'un fournisseur hébergé. L'exprimer reviendrait à figer ces noms de variables dans un document livré, qui dériverait ensuite du code qui les possède.

C'est la liste complète. Une configuration que l'éditeur affiche comme propre peut encore échouer sur l'une de ces trois règles : un éditeur au vert est donc une première passe rapide, pas un substitut à `verbatra check`.

## Le répertoire de travail et .env

Lance verbatra depuis la racine de ton projet, ou passe `--cwd` pointant dessus. Le répertoire de travail est l'ancre unique de tout ce que verbatra résout : la recherche de configuration y démarre et remonte, `.env.local` et `.env` y sont chargés pour toutes les commandes sauf `init`, et chaque chemin de fichier de locale se résout contre lui. Garder les trois au même endroit est ce qui fait qu'un simple `verbatra translate` fonctionne.

La précédence d'environnement est, du plus fort au plus faible : une variable déjà définie dans ton environnement réel, puis `.env.local`, puis `.env`. Une variable déjà présente n'est jamais écrasée.

## Quand le chargement échoue

Le chargement de la configuration échoue avec l'un de deux codes d'erreur structurés :

- **`CONFIG_NOT_FOUND`** : la recherche n'a trouvé aucune configuration à aucun des endroits ci-dessus, ou le chemin explicite `--config` n'existe pas. Sur les commandes qui détectent, une recherche infructueuse n'échoue pas ici : elle bascule vers la [détection](#fonctionner-sans-fichier-de-configuration), qui rapporte ses propres codes. Un chemin `--config` explicite qui n'existe pas échoue toujours, et `doctor` et `studio` ne détectent jamais.
- **`CONFIG_INVALID`** : une configuration a été trouvée mais n'a pas pu être utilisée. Cela couvre un fichier qui ne s'analyse pas, une configuration qui échoue à la validation de schéma, et un chemin de fichier de glossaire qui n'a pas pu être résolu. Le message liste chaque problème de validation avec son chemin de clé, et une clé de premier niveau non reconnue reçoit en plus l'indice que les clés d'API sont lues depuis l'environnement, pas depuis la configuration. Une erreur brute d'analyseur ou de système de fichiers ne s'échappe jamais.

La CLI rapporte l'un ou l'autre échec et sort avec le code `2`, le code de sortie des erreurs de frontière. Voir [CI et codes de sortie](/docs/ci-and-exit-codes).
