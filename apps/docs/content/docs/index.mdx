---
title: Getting Started
description: Install verbatra, scaffold a config, and provide your provider key.
---

# Getting Started

verbatra is a CLI and SDK that keeps your i18n locale files in sync. You maintain the source locale, and it translates only the keys that changed through your choice of AI or machine-translation provider.

This guide takes you from an empty project to a verbatra setup that is ready to run: install the CLI, scaffold a config, and provide your provider key. Running your first translation is the next page.

## Requirements

Node.js `>=22.14.0`.

## Install

verbatra is a development dependency:

```bash
pnpm add -D @verbatra/cli
# npm
npm install -D @verbatra/cli
# yarn
yarn add -D @verbatra/cli
```

Run the binary through your package manager: `pnpm verbatra ...`, `npx verbatra ...`, or `yarn verbatra ...`.

## Scaffold a config

Run `init` and choose your provider:

```bash
verbatra init --provider anthropic
```

This writes a `verbatra.config.ts` and a `.env.example`, and it adds `.env` and `.env.local` to your `.gitignore` so a real key is never committed. With a terminal attached, `init` prompts for the values it cannot default; pass `--yes` to accept the defaults and run without prompts. The defaults are `en` for the source locale, `de` for the target locales, and `locales/{locale}.json` for the file pattern.

## Provide the API key

Each provider reads its key from one environment variable. For Anthropic that is `ANTHROPIC_API_KEY`. Set it in `.env` (which `init` has already gitignored) or export it in your shell:

```bash
export ANTHROPIC_API_KEY=your-key-here
```

verbatra loads `.env.local` and then `.env` from the working directory before it runs, and a variable already present in your environment always wins. See [Providers](/docs/providers) for each provider's variable.

## Next

- [Your first translation](/docs/your-first-translation): run verbatra, read the run summary, and see what "translate only what changed" means in practice.
- [Configuration](/docs/config-file): the full config schema and discovery order.
