---
title: Introduction
description: Keep your locale files in sync. Translate only what changed.
---

<div className="not-prose flex flex-col gap-5 pt-2 pb-4">
  <div className="flex items-center gap-3">
    <VMark size={44} />
    <span className="font-mono text-sm tracking-[0.2em] text-fd-muted-foreground">verbatra docs</span>
  </div>
  <h1 className="max-w-2xl text-balance text-4xl font-semibold tracking-tight text-fd-foreground md:text-5xl" style={{ fontFamily: "var(--font-display)" }}>Translate only what changed.</h1>
  <div className="max-w-2xl text-lg text-fd-muted-foreground">You keep the source locale. verbatra translates every other locale through your provider, and on each run it sends only the keys that changed since the last one.</div>
  <CopyCommand command="pnpm add -D @verbatra/cli" />
</div>

<DiffPanel />

<LaneCards />

<ReferenceRow />
