---
"@verbatra/core": minor
---

Add 'ngx-translate-json' to SupportedFormat, so the format-adapters package can declare an
ngx-translate adapter. Additive only; existing format values are unchanged.
