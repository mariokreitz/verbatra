---
"@verbatra/studio": patch
---

Fix the README quick-start example. Run the dashboard with `npx verbatra studio` instead of the bare `verbatra studio`, which fails when the binary is not on your PATH.
