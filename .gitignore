# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# verbatra team workspace (audit trail, specs, ADRs, and agent memory; local only)
.verbatra

# verbatra's own translation-memory cache for apps/docs: local, regenerable, never committed.
# That project was scaffolded before `verbatra init` wrote this entry, so it was never added here.
verbatra.cache.json

# Next.js / Fumadocs (docs app build artifacts)
.next/
.source/

# Turborepo
.turbo/

# Test-Coverage
coverage/

# Environment variables and local overrides
.env
.env.*
*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/

# pnpm project-local store (normally global)
.pnpm-store/

# npm/yarn lockfiles (this repo uses pnpm)
package-lock.json
!e2e/package-lock.json
yarn.lock
